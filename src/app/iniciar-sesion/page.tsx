import { AuthForm } from '@/componentes/autenticacion/formulario-inicio';

export default function LoginPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4">
      <AuthForm />
    </main>
  );
}
